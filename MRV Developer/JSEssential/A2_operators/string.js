// return lenght to string
const textSize = 'Texto'
console.log((textSize.length))

// return an array broken with an delimitator
const splittedText = 'Texto'
console.log(splittedText.split('x'))

// Research  an value and replace by other
const replacedText = 'Texto'
console.log(replacedText.replace('Text', 'txeT'))

//Return an slice to value
const lastChar = 'Texto'
console.log(lastChar.slice(-1))

const allWithoutLastChar = 'Texto'
console.log(allWithoutLastChar.slice(0, -1))

const secondToEnd = 'Texto'
console.log(secondToEnd.slice(1))

// return N charactres from position
const twoCharsBeforeFirstPosition = 'Texto'
console.log(twoCharsBeforeFirstPosition.substr(0, 2))

const twoCharsBeforeFirstPosition = 'Texto'
console.log(twoCharsBeforeFirstPosition.substr(1, 3))